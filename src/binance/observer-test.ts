// import { Observer } from "./observer"

// setTimeout(async () => {
//     // const minVolumeOverlap = 

//     // the following parameters need to be supplied in case of a restart scenario
//     // const currentInvestmentSymbol = "OAXBTC"
//     // const currentlyInvestedUnits = 178
//     // await Observer.observe(intervalLengthInSeconds, maxNumberOfPatienceIntervals, currentInvestmentSymbol, currentlyInvestedUnits)
//     const size = Number(process.argv[2])
//     const difference = Number(process.argv[3])
//     const intervalLengthInSeconds = Number(process.argv[4])
//     const maxNumberOfPatienceIntervals = Number(process.argv[5])

//     console.log(`starting with size: ${size} - difference: ${difference} - intervalLengthInSeconds: ${intervalLengthInSeconds}`)
//     await Observer.observe(intervalLengthInSeconds, maxNumberOfPatienceIntervals, size, difference)
// })
